#include "Actor.h" // 선언된 Actor 헤더 파일을 포함하여 구현을 시작합니다.

Actor::Actor() // 액터 생성자의 구현부입니다.
{ // 생성자 로직 시작입니다.
    // 현재는 액터 생성 시 특별히 초기화할 동적 메모리 등이 없으므로 비워둡니다.
} // 생성자 로직 끝입니다.

Actor::~Actor() // 액터 소멸자의 구현부입니다.
{ // 소멸자 로직 시작입니다.
    // 현재는 직접 해제할 자원이 없으므로 비워둡니다.
} // 소멸자 로직 끝입니다.

void Actor::Update(float deltaTime) // 액터의 매 프레임 갱신 함수 구현부입니다.
{ // 함수 블록 시작입니다.
    // 가장 기본적인 베이스 액터는 매 프레임 아무런 특수 행동도 하지 않습니다.
    // 이 함수는 'CubeActor' 같은 자식 클래스에서 오버라이드(덮어쓰기)하여 실제 로직을 구현하게 됩니다.
} // 함수 블록 끝입니다.